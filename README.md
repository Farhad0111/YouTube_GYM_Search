1. Set The 'GROQ_API_KEY' & 'YOUTUBE_API_KEY' (https://console.cloud.google.com/apis/credentials?project=gen-lang-client-0629568773)
2. In the Terminal install 'python -m venv .venv'
3. In the Treminal '.venv\scripts\activate' or 'venv\scripts\activate'
4. In the ,Terminal install 'pip install -r requirements.txt'
5. To launch the live app, just run: 'python -m streamlit run main_yt.py' in the terminal.
