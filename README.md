Set The 'GROQ_API_KEY' & 'YOUTUBE_API_KEY' (https://console.cloud.google.com/apis/credentials?project=gen-lang-client-0629568773)
In the Terminal install 'python -m venv .venv'
In the Treminal '.venv\scripts\activate' or 'venv\scripts\activate'
In the ,Terminal install 'pip install -r requirements.txt'
To launch the live app, just run: 'python -m streamlit run main_yt.py' in the terminal.
